import React from "react";

const FileExplorer = ({ sideBarWidth }) => {
  return (
    <div
      className=""
      style={{
        width: `${sideBarWidth}px`,
      }}
    >
      This is reserved for sidebar only yeah
    </div>
  );
};

export default FileExplorer;
